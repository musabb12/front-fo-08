# تقييم جودة التطبيق المُطوَّر بمساعدة الذكاء الاصطناعي

**المشروع:** `agency-web-dev-service-foxy`
**تاريخ المراجعة:** 2026-08-27
**النتيجة المعتمدة (تدقيق صارم): 56 / 100 — Prototype-Grade — غير جاهز للإنتاج**
**النتيجة القياسية (تقييم أولي): 73 / 100 — جيد جدًا**

> الرقمان صحيحان ويقيسان شيئين مختلفين: **73** يقيس جودة الكود المكتوب فعلًا،
> و**56** يقيس جاهزية النظام للإنتاج والتوسع. المعتمد عند الحكم على التسليم هو **56**.
> التفصيل الكامل للتدقيق الصارم في آخر المستند.

---

## نظرة عامة

| البند | القيمة |
|---|---|
| نوع التطبيق | صفحة خدمة واحدة (Landing / Service Page) بالعربية RTL |
| الستاك | Next.js 14.2 (App Router) + React 18 + TypeScript 5.5 + Tailwind CSS 3.4 |
| حجم الكود | 18 ملف مصدري (خارج `node_modules`) — 11 مكوّن، ملف بيانات واحد، ملف `lib` واحد |
| المسار الرئيسي | `/services/web-development` |
| عدد الـ Commits | 8 commits بصيغة Conventional Commits |

**طريقة المراجعة:** قراءة كاملة لكل ملف مصدري في المستودع (بدون استثناء)، بالإضافة إلى
`package.json`، `tsconfig.json`، `next.config.mjs`، `tailwind.config.ts`، `README.md`،
سجل Git، ومخرجات البناء في `.next/`. كل ملاحظة أدناه مربوطة بملف وسطر فعلي — لا توجد
ملاحظات مبنية على تخمين.

---

## التقييم التفصيلي

### 1. جودة الكود وهندسته — الدرجة: 19 / 25

**نقاط القوة**
- `tsconfig.json` مفعّل عليه `strict: true` **و** `noUncheckedIndexedAccess: true` — ده مستوى
  صرامة أعلى من المتوسط، وأغلب المشاريع المولّدة بالذكاء الاصطناعي بتسيبه على الإعداد الافتراضي.
- فصل نظيف بين المحتوى والعرض: كل النصوص الأساسية في `data/services/web-development.ts`
  وبتوصل للمكوّنات كـ props، فالمكوّنات كلها Presentational خالصة.
- استخدام `satisfies` على مصفوفات البيانات بدل `as` — إثبات نوع بدون فقدان الاستدلال (type inference).
- مافيش أي مكوّن أطول من 110 سطر، ومافيش تكرار كتل كود واضح.

**أهم المشاكل**
- **الدومين وهمي ومكرر في مكانين**: `app/layout.tsx:29` فيه
  `metadataBase: new URL("https://example-agency.com")`، و`lib/schema.ts:3` فيه نفس القيمة في
  `SITE_URL`. يعني الـ canonical و Open Graph و JSON-LD كلهم بيشيروا لدومين غير موجود، والقيمة
  متكررة بدل ما تكون ثابت واحد.
- **`npm run lint` مش شغّال أصلًا**: السكربت موجود في `package.json`، لكن مافيش حزمة `eslint`
  في الـ dependencies، ولا في `node_modules`، ولا أي ملف `.eslintrc*` أو `eslint.config.*`.
  السكربت ده بيدّي إحساس زائف بوجود فحص جودة.
- **رابط مكسور**: `components/ui/Button.tsx` بيرندر `<a>` عادي، و`ServiceCta.tsx:15` بيستخدم
  `href="/contact"` — والمسار ده **غير موجود في المشروع**، فالزر الرئيسي للتحويل بيودّي لـ 404،
  وكمان بيعمل إعادة تحميل كاملة للصفحة بدل التنقل الداخلي.
- **البناء الإنتاجي لم يُختبر أبدًا**: مجلد `.next/` فيه مخرجات وضع التطوير فقط (مافيش `BUILD_ID`
  ولا `.next/server/app`)، والـ README نفسه بيعترف: *"الكود مش شغّال Build فعلي هنا"*.
- **مافيش أي اختبارات** (لا وحدة ولا تكامل ولا smoke test)، ومافيش CI.
- **الأنواع (Interfaces) ساكنة في ملف البيانات**: `Faq`, `Capability`, `ProcessStep`... معرّفة داخل
  `data/services/web-development.ts` وبيستوردها `lib/schema.ts` وكل المكوّنات — ده عكس اتجاه
  الاعتمادية الصحيح؛ ملف البيانات المفروض يستهلك الأنواع مش يكون مصدرها.
- الكائن الرئيسي `webDevelopmentService` مالوش `interface` من الأعلى، فمافيش عقد يمنع صفحة خدمة
  تانية من نسيان حقل مطلوب.
- `dangerouslySetInnerHTML` في `page.tsx` بيحقن JSON-LD من غير تهريب `<` (خطورة منخفضة هنا لأن
  المحتوى ثابت، لكنها مش الممارسة الصحيحة).

**التحسينات المطلوبة**
1. ثابت واحد `SITE_URL` مصدره `process.env.NEXT_PUBLIC_SITE_URL` يستهلكه `layout.tsx` و`schema.ts`.
2. تثبيت `eslint` + `eslint-config-next` وتشغيل `npm run lint` فعليًا، أو حذف السكربت.
3. إنشاء `/contact` أو تغيير الرابط لقسم موجود، وتحويل `Button` لاستخدام `next/link` للروابط الداخلية.
4. تشغيل `npm run build` والتأكد من نجاحه قبل أي تسليم.
5. نقل كل الـ interfaces لـ `lib/types.ts` وإضافة `interface ServiceContent` للكائن الرئيسي.

---

### 2. SEO والأداء التقني — الدرجة: 9.5 / 15

**نقاط القوة**
- استخدام صحيح ومتقدّم لـ Metadata API في `app/services/web-development/page.tsx:17-36`:
  `title`, `description`, `alternates.canonical`, Open Graph, Twitter Card، مع `template`
  للعنوان في `layout.tsx`.
- **Structured Data حقيقية**: `Service` schema و`FAQPage` schema كـ JSON-LD مبنية عبر دوال
  في `lib/schema.ts` — دي نقطة قوة نادرة في التسليمات المولّدة بالذكاء الاصطناعي.
- ترتيب عناوين سليم: `h1` واحد فقط في الـ Hero، و`h2` لكل قسم، و`h3` للعناصر الفرعية.
- `lang="ar"` و`dir="rtl"` مضبوطين على `<html>` مباشرة.
- الصفحة كلها Server Components ماعدا الأكورديون — يعني JS bundle صغير جدًا. مافيش صور نهائيًا،
  فمافيش مشاكل تحسين صور.

**أهم المشاكل**
- **الدومين الوهمي بيبطّل كل شغل الـ SEO**: الـ canonical هيطلع
  `https://example-agency.com/services/web-development` — ده أخطر من عدم وجود canonical أصلًا.
- **مافيش `sitemap.xml` ولا `robots.txt`**: مافيش `app/sitemap.ts` ولا `app/robots.ts`.
- **مجلد `public/` فاضي تمامًا**: مافيش favicon، ومافيش صورة Open Graph — رغم إن
  `twitter.card` معلن كـ `summary_large_image`، فالكارت هيظهر بدون صورة.
- **إجابات الأسئلة الشائعة مش موجودة في الـ HTML**: في `ServiceFaqs.tsx:36` الشرط `{isOpen && ...}`
  بيلغي العنصر من الـ DOM بالكامل، فمحتوى 3 من 4 أسئلة غير موجود في الصفحة المُقدَّمة. الـ JSON-LD
  بيعوّض جزئيًا، لكن المحتوى المرئي نفسه مش قابل للزحف.
- **حِمل الخطوط هو أكبر تكلفة أداء في الصفحة**: `app/layout.tsx` بيحمّل 3 عائلات خطوط بـ 8 أوزان
  (Cairo ×3، IBM Plex Sans Arabic ×3، IBM Plex Mono ×2) مع subsets عربية — والـ subsets العربية
  ثقيلة جدًا. ده لصفحة نصية بحتة.
- **مافيش أي Security Headers** في `next.config.mjs` (لا CSP ولا HSTS ولا X-Frame-Options) — رغم
  إن محتوى الصفحة نفسه بيسوّق لـ *"أمان مطبّق من التصميم"*.

**التحسينات المطلوبة**
1. `NEXT_PUBLIC_SITE_URL` بدل الدومين الوهمي (أولوية قصوى).
2. إضافة `app/sitemap.ts` و`app/robots.ts` و`app/icon.png` و`app/opengraph-image.png`.
3. تغيير `{isOpen && ...}` إلى `<div hidden={!isOpen}>` عشان النص يفضل في الـ DOM.
4. تقليل الخطوط لعائلتين بحد أقصى (خط عرض + خط نص)، وتقليل الأوزان لـ 2 لكل عائلة.
5. إضافة `async headers()` في `next.config.mjs` بالهيدرز الأمنية الأساسية.

---

### 3. التعليقات ووضوح الكود — الدرجة: 7 / 10

**نقاط القوة**
- التسمية ممتازة ومتسقة: `ServiceHero`, `ServiceCapabilities`, `buildFaqSchema`, `BlueprintSchematic`
  — كل اسم بيشرح نفسه من غير تعليق.
- فيه تعليقات بتشرح **"ليه"** مش "إيه"، وده المستوى الصح: التعليق فوق `ServicePortfolio.tsx:4-8`
  بيشرح ليه فيه Empty State بدل دراسة حالة مخترعة — قرار منتج موثّق في الكود.
- التعليقات داخل `globals.css` وداخل الـ SVG في `ServiceHero.tsx` بتوجّه القارئ بشكل مفيد.

**أهم المشاكل**
- **تعليقان يناقضان الكود فعليًا**:
  - `components/ui/Section.tsx:12-14` بيقول *"Every section on the page renders through this so
    spacing never drifts"* — بينما `ServiceIntro.tsx` و`ServiceCta.tsx` و`ServiceHero.tsx`
    بيتخطوه ويعيدوا كتابة نفس الـ classes يدويًا.
  - نفس النوع من التناقض في README (مذكور في البند 4).
- **تعليقات ميتة**: `// eslint-disable-next-line react/no-danger` مرتين في
  `app/services/web-development/page.tsx` — وESLint مش مثبّت أصلًا.
- **مراجع معلّقة**: تعليقات بتشير لـ `(see proposal §UI System)` و`(proposal §SEO)` — والمستند ده
  مش موجود في المستودع، فالقارئ الجديد مش هيقدر يوصله.
- 5 مكوّنات من 11 (`ServiceIntro`, `ServiceCta`, `ServiceBenefits`, `ServiceCapabilities`,
  `ServiceDeliverables`) بدون أي تعليق توضيحي.

**التحسينات المطلوبة**
1. تصحيح تعليق `Section.tsx` أو — الأفضل — تصحيح الكود ليطابق التعليق.
2. حذف تعليقات `eslint-disable` أو تثبيت ESLint.
3. استبدال مراجع "proposal §" بروابط لأقسام داخل `README.md`.

---

### 4. ملفات التوثيق (Documentation) — الدرجة: 8 / 10

**نقاط القوة**
- `README.md` من أفضل ما يمكن في تسليم بهذا الحجم، ومقسّم بذكاء لجمهورين:
  **قسم للعميل غير المبرمج** و**قسم للمطوّر** — ده تفكير احترافي مش تلقائي.
- بيحتوي على شجرة المجلدات كاملة مع شرح دور كل ملف.
- بيوثّق **Design Tokens** ويفرض قاعدة صريحة: ممنوع hex مباشر، استخدم التوكنز.
- بيوثّق شغل الـ SEO والـ a11y المطبّق بالتفصيل.
- أمانة في الطرح: بيعترف صراحةً إن البناء لم يُشغَّل في بيئة التسليم.

**أهم المشاكل**
- **ادعاء غير صحيح**: README بيقول *"كل النصوص موجودة في ملف واحد: `data/services/web-development.ts`
  — أي تعديل ... من غير لمس أي كود تاني"*. ده **غلط**: عناوين الأقسام وسطور الـ eyebrow كلها
  مكتوبة داخل المكوّنات (تفاصيل في البند 6). العميل اللي هيصدّق الجملة دي هيتعطّل.
- مافيش شرح لـ **"إزاي تضيف صفحة خدمة جديدة"** — وده أهم سؤال هيسأله المطوّر التالي.
- مافيش `.env.example`، ومافيش توثيق لمتغيرات البيئة (المفروض يكون فيه واحد على الأقل للدومين).
- مافيش `CHANGELOG` ولا `LICENSE` ولا `CONTRIBUTING`.

**التحسينات المطلوبة**
1. تصحيح جملة "كل النصوص في ملف واحد" فورًا — أو تنفيذها فعليًا في الكود.
2. إضافة قسم "خطوات إضافة صفحة خدمة جديدة" في README.
3. إضافة `.env.example` فيه `NEXT_PUBLIC_SITE_URL`.

---

### 5. هيكلة الصفحات والمجلدات (Architecture) — الدرجة: 11.5 / 15

**نقاط القوة**
- استخدام صحيح لـ App Router: `app/layout.tsx` كـ shell، والصفحة في مسار متداخل يطابق الـ URL.
- تقسيم منطقي واضح: `components/ui/` (عام قابل لإعادة الاستخدام) مقابل `components/services/`
  (خاص بصفحات الخدمات) — ده تقسيم صحيح ومقصود.
- `data/` و`lib/` منفصلين عن طبقة العرض تمامًا.
- `paths: { "@/*": ["./*"] }` مضبوط، وكل الاستيرادات مطلقة ونظيفة — مافيش `../../..`.

**أهم المشاكل**
- **مافيش مسار ديناميكي `[slug]`**: الصفحة بتستورد `webDevelopmentService` بشكل صريح ومباشر
  (`page.tsx:2` ثم `const service = webDevelopmentService`). إضافة خدمة تانية = نسخ المجلد كله
  ولصقه. لوكالة بتقدّم عدة خدمات، ده أكبر عيب معماري في المشروع.
- **الصفحة الرئيسية عبارة عن سقالة تجريبية**: `app/page.tsx` فيها نص *"تاسك تصميم صفحة"* وسهم
  بيشاور على مسار الخدمة — ده محتوى تطوير اتشحن مع التسليم.
- **مافيش `not-found.tsx` ولا `error.tsx`** على مستوى الـ app — أي خطأ هيوصل للمستخدم بشاشة Next الافتراضية.
- **`public/` فاضي بالكامل** — مافيش favicon حتى.
- اتجاه الاعتمادية مقلوب: `lib/schema.ts` بيستورد النوع `Faq` من `data/` (المفروض العكس أو من `lib/types.ts`).
- تسرّب حالة في المستودع وقت التسليم: `tsconfig.json` معدّل و`package-lock.json` غير مُدرَج في Git.

**التحسينات المطلوبة**
1. تحويل المسار إلى `app/services/[slug]/page.tsx` مع `generateStaticParams()` وسجل خدمات في `data/services/index.ts`.
2. استبدال الصفحة الرئيسية بمحتوى حقيقي أو إعادة توجيه.
3. إضافة `app/not-found.tsx` و`app/error.tsx` و`app/icon.png`.
4. `git add package-lock.json` وتثبيت حالة المستودع قبل التسليم.

---

### 6. تقسيم وإعادة استخدام المكونات (Components) — الدرجة: 11 / 15

**نقاط القوة**
- وجود **Primitives حقيقية**: `Section` بيوحّد إيقاع الأقسام (eyebrow → h2 → محتوى)،
  و`Button` بيوحّد كل الـ CTAs بنظام variants (`primary` / `outline`) عبر خريطة
  `Record<ButtonVariant, string>` — تصميم نظيف وقابل للتوسع.
- `Button` بيوسّع `ComponentPropsWithoutRef<"a">`، فبيقبل كل خصائص الرابط بشكل نوعي سليم.
- كل مكوّنات الأقسام بتستقبل بياناتها كـ props — مافيش مكوّن بيقرأ من ملف البيانات مباشرة
  (ماعدا استثناء واحد، تحت).
- كل مكوّن مسؤولية واحدة واضحة، ومافيش مكوّن "God Component".

**أهم المشاكل**
- **3 أقسام من 9 بتتخطى `Section`**: `ServiceIntro.tsx:8`، `ServiceCta.tsx:12`، و`ServiceHero.tsx:88`
  بيعيدوا كتابة `mx-auto max-w-6xl px-6 py-16` يدويًا — يعني الـ Primitive الأساسي متجاوَز في ثلث الحالات،
  وأي تعديل على عرض الحاوية هيحتاج تعديل في 4 أماكن.
- **نصوص العناوين مدفونة داخل المكوّنات**: كل `eyebrow` و`title` مكتوب hardcoded —
  `ServiceCapabilities.tsx:10`, `ServiceProcess.tsx:10`, `ServiceDeliverables.tsx:10`,
  `ServiceBenefits.tsx:10`, `ServiceFaqs.tsx:15`, `ServicePortfolio.tsx:11`. النتيجة: المكوّنات دي
  **غير قابلة لإعادة الاستخدام** في صفحة خدمة تانية، وده اللي بيكسر الادعاء الموجود في README.
- **`ServicePortfolio` لا يستقبل أي props إطلاقًا** ونصوصه كلها مكتوبة جواه — مكوّن مقفول تمامًا.
- **`Button` مقفول على `<a>`**: مافيش دعم لـ `next/link` ولا لـ `<button>` حقيقي، فأي CTA
  بيفتح مودال أو يبعت فورم مش هيقدر يستخدم نفس المكوّن.
- مكوّن `BlueprintSchematic` (83 سطر SVG) محشور جوه ملف `ServiceHero.tsx` بدل ما يكون
  `components/ui/BlueprintSchematic.tsx`.

**التحسينات المطلوبة**
1. تمرير `eyebrow` و`title` كـ props من ملف البيانات لكل قسم.
2. تمرير `ServiceIntro` و`ServiceCta` و`ServiceHero` عبر `Section` أو استخراج مكوّن `Container`.
3. جعل `Button` polymorphic: `next/link` للروابط الداخلية، `<a>` للخارجية، `<button>` للأفعال.
4. نقل `ServicePortfolio` لاستقبال محتواه من `data/`.

---

### 7. النظافة والاتساق وقابلية الصيانة — الدرجة: 7 / 10

**نقاط القوة**
- سجل Git نظيف بصيغة Conventional Commits ونطاقات صحيحة:
  `feat(seo):`, `feat(ui):`, `docs:`, `fix(hero):`, `chore:` — 8 commits كل واحد بمسؤولية واحدة.
- `.gitignore` سليم ومكتمل (`node_modules/`, `.next/`, `.env`, `.DS_Store`).
- اتساق كامل في نمط الكتابة: named exports في كل مكان، نفس ترتيب `interface` ثم المكوّن،
  نفس طريقة كتابة الـ Tailwind classes.
- تباين الألوان يجتاز WCAG AA بفرق مريح (تم الحساب: `muted` على `canvas` ≈ 6.8:1،
  على `canvas-raised` ≈ 6.1:1، و`amber` على `canvas` ≈ 8.7:1) — نقطة جودة حقيقية.

**أهم المشاكل**
- **خرق قاعدة التوكنز اللي المشروع نفسه فرضها**: `ServiceHero.tsx` فيه `#F2B134` مكتوب يدويًا
  7 مرات (الأسطر 35، 39، 42، 43، 76) — بينما README بيقول صراحةً *"استخدم التوكنز بدل ألوان hex مباشرة"*.
  لو اتغيّر لون العلامة التجارية، الـ SVG هيفضل على اللون القديم.
- **مافيش Prettier ولا أي formatter**، ومافيش `.editorconfig`.
- **مافيش CI**: مافيش `.github/workflows` — فمافيش أي بوابة بتمنع كود مكسور من الدمج.
- بقايا: تعليقات eslint ميتة، صفحة رئيسية تجريبية، ملفات معدّلة غير مثبتة في Git وقت التسليم.
- ملاحظة دلالية صغيرة: الـ Hero ملفوف في `<header>`، وده بينشئ landmark من نوع `banner` على مستوى
  الصفحة لقسم محتوى — الأنسب `<section>`.

**التحسينات المطلوبة**
1. تمرير لون التوكن للـ SVG عبر `currentColor` أو CSS variable بدل الـ hex.
2. إضافة Prettier + `.editorconfig`.
3. إضافة GitHub Action بسيط: `tsc --noEmit` + `next lint` + `next build`.

---

## جدول الدرجات

| المعيار | الوزن | الدرجة | النسبة |
|---|:---:|:---:|:---:|
| جودة الكود وهندسته | 25 | **19** | 76% |
| SEO والأداء التقني | 15 | **9.5** | 63% |
| التعليقات ووضوح الكود | 10 | **7** | 70% |
| ملفات التوثيق (Documentation) | 10 | **8** | 80% |
| هيكلة الصفحات والمجلدات | 15 | **11.5** | 77% |
| تقسيم وإعادة استخدام المكونات | 15 | **11** | 73% |
| النظافة والاتساق وقابلية الصيانة | 10 | **7** | 70% |
| **الإجمالي** | **100** | **73** | **73%** |

---

## نتيجة التقييم القياسي: 73 / 100 — جيد جدًا

تسليم قوي ومدروس، وواضح إن وراه تفكير هندسي مش مجرد توليد سريع: نظام تصميم حقيقي بتوكنز،
فصل صحيح بين المحتوى والعرض، TypeScript صارم بإعدادات فوق المتوسط، شغل a11y فعلي (وليس ادّعاء)،
Structured Data مبنية بشكل صحيح، وREADME موجّه لجمهورين. دي مؤشرات نضج مش شائعة في التسليمات
المولّدة بالذكاء الاصطناعي.

لكن التطبيق **غير جاهز للإطلاق** بحالته الحالية. المشاكل مش في التصميم أو الفكرة، لكن في
"الميل الأخير": دومين وهمي بيبطّل كل شغل الـ SEO، سكربت lint بيوهم بوجود فحص مش موجود، بناء
إنتاجي لم يُشغَّل ولا مرة، ورابط التحويل الرئيسي بيودّي لصفحة 404. والنمط المتكرر الأوضح هو
**فجوة بين ما يوثّقه الكود عن نفسه وما ينفّذه فعلًا** — التعليقات وREADME بيوصفوا نسخة أنضف من
الموجودة. ده أخطر من عيب تقني، لأنه بيضلّل المطوّر التالي.

الخلاصة: الأساس ممتاز، والمطلوب يوم عمل واحد مركّز على بنود P0 للوصول لمستوى إنتاجي.

---

## أهم 5 مشاكل

| # | المشكلة | الموقع | الأثر |
|---|---|---|---|
| 1 | دومين وهمي `example-agency.com` مكرر في مكانين — canonical و OG و JSON-LD كلهم بيشيروا لموقع غير موجود | `app/layout.tsx:29`، `lib/schema.ts:3` | **حرج** — كل شغل الـ SEO مُلغى عمليًا |
| 2 | `npm run lint` سكربت وهمي (ESLint غير مثبّت)، صفر اختبارات، صفر CI، والبناء الإنتاجي لم يُشغَّل | `package.json`، `.next/` | **حرج** — مافيش أي بوابة جودة |
| 3 | المحتوى **غير** مركزي فعليًا: عناوين كل الأقسام مكتوبة داخل المكوّنات، بعكس ما يدّعيه README | 6 مكوّنات + `README.md` | **عالي** — يكسر إعادة الاستخدام ويضلّل العميل |
| 4 | مافيش sitemap ولا robots ولا favicon ولا صورة OG، وإجابات الـ FAQ محذوفة من الـ DOM | `app/`، `public/`، `ServiceFaqs.tsx:36` | **عالي** — محتوى وأصول أساسية ناقصة للزحف |
| 5 | زر التحويل الرئيسي بيودّي لـ `/contact` غير الموجود، و`Button` بيستخدم `<a>` بدل `next/link` | `ServiceCta.tsx:15`، `Button.tsx:20` | **عالي** — 404 على أهم مسار تحويل |

---

## أولويات التحسين

### P0 — قبل أي إطلاق (يوم عمل واحد)
1. `NEXT_PUBLIC_SITE_URL` كثابت واحد يستهلكه `layout.tsx` و`lib/schema.ts`.
2. إنشاء صفحة `/contact` أو تغيير رابط الـ CTA لهدف موجود.
3. تشغيل `npm run build` والتأكد من نجاحه (وتوثيق النتيجة).
4. إضافة `app/sitemap.ts` + `app/robots.ts` + `app/icon.png` + `app/opengraph-image.png`.
5. تثبيت `eslint` + `eslint-config-next` وتشغيله فعليًا، أو حذف السكربت الوهمي.
6. تصحيح جملة "كل النصوص في ملف واحد" في README.
7. استبدال الصفحة الرئيسية التجريبية `app/page.tsx`.

### P1 — خلال أسبوع
1. نقل `eyebrow` و`title` لكل الأقسام إلى `data/services/web-development.ts`.
2. تمرير `ServiceIntro` و`ServiceCta` و`ServiceHero` عبر `Section` (أو `Container`).
3. `Button` polymorphic بـ `next/link` للروابط الداخلية.
4. `<div hidden={!isOpen}>` بدل `{isOpen && ...}` في الأكورديون.
5. تحويل ألوان الـ SVG لتوكنز عبر `currentColor`.
6. تقليل الخطوط من 3 عائلات / 8 أوزان إلى عائلتين / 4 أوزان.
7. `async headers()` في `next.config.mjs` بالهيدرز الأمنية.
8. تصحيح تعليق `Section.tsx` وحذف تعليقات eslint الميتة.

### P2 — تحسين هيكلي
1. `app/services/[slug]/page.tsx` + `generateStaticParams()` + سجل خدمات في `data/services/index.ts`.
2. نقل كل الـ interfaces إلى `lib/types.ts` وإضافة `interface ServiceContent`.
3. `app/not-found.tsx` و`app/error.tsx`.
4. Prettier + `.editorconfig` + GitHub Action (`tsc --noEmit` + `lint` + `build`).
5. اختبار smoke واحد على الأقل للصفحة، واختبار وحدة لدوال `lib/schema.ts`.
6. استخراج `BlueprintSchematic` لملف مستقل في `components/ui/`.

---

# ملحق: نظام التصميم (Design System) وتصميم النظام (System Design)

> إضافة على طلب لاحق. المصطلح "system design" له قراءتان في سياق المشروع ده، والاتنين مغطّيين هنا:
> **(أ)** نظام التصميم البصري، و**(ب)** التصميم الهندسي للنظام ككل.
> **ملاحظة:** الملحق ده **لا يغيّر النتيجة القياسية 73/100** — لأن أثره محسوب أصلًا داخل
> المعايير 5 و6 و7. الغرض منه إعطاء حكم مستقل ومفصّل على النقطتين.

---

## (أ) نظام التصميم — هل هو موجود؟

**الإجابة المختصرة: نعم، موجود فعلًا — لكنه ناقص. نظام على مستوى التوكنز، وليس على مستوى المكوّنات.**

**درجة النضج: 5.5 / 10 — "بداية نظام" وليس نظام مكتمل.**

ده مش "مافيش نظام"، وبرضه مش نظام تصميم حقيقي. هو **طبقة توكنز محترمة فوقها مكتبة مكوّنات شبه غائبة**.

### الطبقات الموجودة فعلًا ✅

| الطبقة | الحالة | الدليل |
|---|---|---|
| توكنز الألوان | **جيدة** | `tailwind.config.ts` — 6 توكنز |
| توكنز الخطوط | **جيدة** | `display` / `body` / `mono` |
| الطبقة الأساسية (Base) | **جيدة** | `globals.css` — focus, reduced-motion, defaults |
| موتيف بصري موحّد | **موجود** | `.blueprint-frame` كـ `@layer components` |
| مكتبة المكوّنات | **ضعيفة جدًا** | مكوّنان فقط: `Button`, `Section` |
| مقياس المسافات (Spacing) | **غير موجود** | — |
| مقياس الخطوط (Type Scale) | **غير موجود** | — |
| مقياس الحواف (Radius) | **غير موجود** | — |
| الظلال / الارتفاع (Elevation) | **غير موجود** | صفر `shadow` في المشروع كله |
| الحركة (Motion) | **غير موجود** | `transition-colors` واحدة فقط |
| الوضع الفاتح/الداكن (Theming) | **غير موجود** | صفر `dark:` — الباليتة داكنة فقط |
| توثيق النظام (Storybook / صفحة توكنز) | **غير موجود** | — |

### أقوى نقطة في النظام: تسمية الألوان دلاليًا

التوكنز مسمّاة **بالدور مش باللون**: `canvas` (الخلفية)، `paper` (النص الأساسي)، `muted` (النص الثانوي)،
`grid` (الحدود)، `amber` (لون الإشارة الوحيد). ده تفكير design-system صحيح ومتقدّم — لو اتغيّرت الهوية
البصرية بالكامل، الأسماء تفضل صحيحة. أغلب المشاريع بتسمّي `blue-500` و`gray-300` وتقع في الفخ ده.

كمان الالتزام بـ **لون إشارة واحد فقط** (`amber`) عبر الصفحة كلها انضباط بصري حقيقي، مش صدفة.

### أخطر ثغرة: مافيش مكوّن `Card` رغم تكرار نمط البطاقة 4 مرات

نفس السطح البصري (`border border-grid bg-canvas-raised`) مكرر بـ 4 صياغات مختلفة يدويًا:

| الملف | الـ classes | الحواف | الحشو |
|---|---|:---:|:---:|
| `ServiceCapabilities.tsx:15` | `blueprint-frame rounded-lg border border-grid bg-canvas-raised p-6` | `lg` | `p-6` |
| `ServiceHero.tsx:104` | `blueprint-frame ... rounded-lg border border-grid bg-canvas-raised p-6` | `lg` | `p-6` |
| `ServicePortfolio.tsx:12` | `blueprint-frame ... rounded-lg border **border-dashed** border-grid bg-canvas-raised p-8` | `lg` | `p-8` |
| `ServiceDeliverables.tsx:15` | `flex ... **rounded-md** border border-grid bg-canvas-raised px-4 py-3` | `md` | `px-4 py-3` |

ده بالظبط تعريف "نظام تصميم ناقص": التوكنز موحّدة، لكن **تركيبها** متروك لكل مكوّن يعيد اختراعه.
النتيجة إن أي تعديل على شكل البطاقات محتاج 4 تعديلات منفصلة، وإن السطر الرابع خرج عن النمط
(`rounded-md` بدل `rounded-lg`) على الأغلب من غير قرار واعٍ.

### باقي الثغرات المؤكدة

- **مافيش إيقاع رأسي موحّد**: 4 قيم مختلفة لحشو الأقسام —
  `py-16 md:py-24` (`Section.tsx:17`)، `py-16` (`ServiceIntro.tsx:8`)، `py-20` (`ServiceCta.tsx:12`)،
  `py-20 md:py-28` (`ServiceHero.tsx:88`). القسم اللي بيدّعي إنه بيوحّد الإيقاع متجاوَز في 3 حالات.
- **مقياس خطوط غير معرّف**: نمط الـ h2 (`text-2xl ... md:text-3xl`) مكتوب يدويًا 3 مرات
  (`Section.tsx:21`، `ServiceIntro.tsx:10`، `ServiceCta.tsx:13`)، و`text-sm` مكرر 7 مرات،
  و`text-lg` 5 مرات. مافيش مكوّن `Heading` أو `Text`.
- **3 قيم حواف بدون توكن**: `rounded` (`ServiceProcess.tsx:14`)، `rounded-md` (`Button`, `ServiceDeliverables`)،
  `rounded-lg` (3 مواضع) — بدون قاعدة تحدد أي واحدة تُستخدم متى.
- **قيم عشوائية خارج المقياس (Magic values)**: `-right-[38px]` و`text-[10px]` في `ServiceProcess.tsx:14`
  — رقمان مضبوطان بالنظر، هيتكسروا مع أي تغيير في حجم الشارة.
- **`Button` فقير كـ primitive**: variantان فقط (`primary`/`outline`)، مافيش `size`، ولا حالة
  `disabled`/`loading`، ولا خانة أيقونة، ولا دعم `next/link` أو `<button>`.
- **خرق التوكنز داخل الـ SVG**: `#F2B134` مكتوب يدويًا 7 مرات في `ServiceHero.tsx` — النظام
  بيفرض قاعدة والـ Hero بيكسرها.
- **صفر توثيق مرئي للنظام**: مافيش Storybook ولا صفحة `/design-system` تعرض التوكنز والمكوّنات.
  التوثيق الوحيد فقرة نصية في README.

### الحكم على نظام التصميم

النية موجودة والأساس صحيح — التوكنز الدلالية والانضباط اللوني مؤشرات على مصمّم/مطوّر بيفكر بمنهجية
نظام مش بمنهجية صفحة. لكن النظام **اتبنى من فوق لتحت وتوقّف عند نص الطريق**: التوكنز اتعرّفت،
والمكوّنات اللي المفروض تستهلكها ما اتبنتش، فرجع الكود يركّب الـ classes يدويًا في كل مكان.

عمليًا: **النظام ده كفاية لصفحة واحدة، وهينهار عند الصفحة الثالثة.**

**الخطوات الثلاث اللي بتحوّله لنظام حقيقي:**
1. استخراج `components/ui/Card.tsx` بـ variants (`solid` / `dashed`) وأحجام حشو — ويستهلكه الأربع مواضع.
2. إضافة `Heading` و`Text` (أو على الأقل توكنز `fontSize` مسمّاة دلاليًا في `tailwind.config.ts`:
   `section-title`, `card-title`, `body`, `caption`) بدل تكرار أحجام Tailwind الخام.
3. توحيد الإيقاع الرأسي في `Section` وإلزام كل الأقسام بالمرور من خلاله (بما فيها Intro و CTA و Hero).

---

## (ب) تصميم النظام (System Design) — هل هو موجود؟

**الإجابة المختصرة: لا يوجد "نظام" أصلًا — والمشروع في حدود نطاقه لا يحتاج واحدًا.**

المشروع ده **صفحة تسويقية ثابتة (Static Brochure Page)**، مش تطبيق. مافيش:

| العنصر | الحالة | هل غيابه مبرَّر؟ |
|---|:---:|---|
| Backend / API | ❌ | ✅ نعم — خارج نطاق التاسك |
| قاعدة بيانات | ❌ | ✅ نعم |
| مصادقة / صلاحيات (Auth / RBAC) | ❌ | ✅ نعم |
| إدارة حالة (State Management) | ❌ | ✅ نعم — `useState` واحدة كفاية |
| جلب بيانات / Caching | ❌ | ✅ نعم — البيانات ثابتة |
| CMS / إدارة محتوى | ❌ | ⚠️ جزئيًا — انظر الملاحظة تحت |
| متغيرات بيئة (`.env`) | ❌ | ❌ **لا** — الدومين لازم يكون متغيّر بيئة |
| معالجة أخطاء (`error.tsx` / `not-found.tsx`) | ❌ | ❌ **لا** — ناقصة فعلًا |
| مسار توسّع للخدمات (`[slug]`) | ❌ | ❌ **لا** — عيب معماري حقيقي |
| إعداد نشر (Vercel / Docker / CI) | ❌ | ❌ **لا** — ناقص للتسليم |
| مراقبة / تحليلات / تتبّع أخطاء | ❌ | ⚠️ مقبول مبدئيًا، مطلوب عند الإطلاق |

### القرارات المعمارية الصحيحة الموجودة ✅

- **استراتيجية الرندر صحيحة تمامًا**: كل الصفحة Server Components ماعدا الأكورديون
  (`"use client"` في `ServiceFaqs.tsx:1` فقط). البيانات ثابت في وحدة برمجية، فالصفحة بتتولّد
  Static بالكامل عند البناء. ده **أفضل اختيار ممكن** لمحتوى زي ده — أسرع رندر وأرخص استضافة
  وأفضل SEO. الحد الفاصل بين Server و Client مرسوم بدقة.
- **طبقة بيانات منفصلة**: `data/` منفصل عن `components/` منفصل عن `lib/` — الفصل ده صحيح
  ولو اتضاف CMS لاحقًا، هيتغيّر ملف واحد بس.
- **`lib/schema.ts` دوال نقية (pure functions)** بتاخد مدخلات وترجّع كائنات — قابلة للاختبار
  مباشرة، ومستقلة عن React.

### الثغرات الحقيقية في حدود النطاق ❌

1. **مافيش نموذج بيانات لـ "مجموعة خدمات"** — الموجود كائن واحد `webDevelopmentService`
   مستورد صراحةً في `page.tsx:2`. النظام ما اتصممش لأكتر من خدمة، وده أول شرط لموقع وكالة.
2. **الإعدادات مدفونة في الكود** — الدومين مكتوب مرتين داخل ملفات مصدرية بدل `.env`.
   ده مش عيب SEO بس، ده عيب تصميم نظام: نفس الكود مش هيشتغل صح في بيئتين مختلفتين
   (staging / production).
3. **مافيش حدود للأخطاء (Error Boundaries)** — أي استثناء وقت الرندر هيوصل للمستخدم خام.
4. **مافيش مسار نشر معرّف** — لا `vercel.json` ولا `Dockerfile` ولا workflow، والبناء
   الإنتاجي نفسه ما اتشغّلش ولا مرة.

### ملاحظة جديرة بالانتباه

محتوى الصفحة نفسه بيسوّق لعميل بـ: *"لوحة تحكم لإدارة المحتوى"*، *"خلفية NestJS قابلة للتوسع"*،
*"RBAC"*، *"بيئة تجريبية (staging)"*، و*"أمان مطبّق من التصميم"*.
التسليم الفعلي مافيهوش أي واحد منهم — ولا حتى الحد الأدنى المقابل لها في نطاقه
(محتوى في ملف كود مش CMS، وصفر security headers).

ده **مبرَّر تمامًا** لأن التاسك كان "صفحة خدمة واحدة" مش بناء المنصة. لكنه يستحق الذكر بوضوح:
**التسليم ده صفحة تصف نظامًا، وليس النظام الموصوف.** والمسافة بين الاتنين لازم تكون مفهومة
للعميل ولأي مطوّر بيستلم الشغل.

### الحكم على تصميم النظام

**لا يوجد system design — وده مقبول في 80% منه لأن النطاق لا يستدعيه.**
المرفوض هو الـ 20% الباقية: غياب متغيرات البيئة، وغياب مسار التوسّع لخدمات متعددة،
وغياب حدود الأخطاء، وغياب إعداد النشر. دي مش "مميزات مؤجلة" — دي الحد الأدنى المتوقّع
من أي صفحة Next.js داخلة الإنتاج.

---

## خلاصة الملحق

| المحور | الحكم | الدرجة |
|---|---|:---:|
| **نظام التصميم (Design System)** | موجود على مستوى التوكنز، ناقص على مستوى المكوّنات. كفاية لصفحة، هينهار عند الثالثة. | **5.5 / 10** |
| **تصميم النظام (System Design)** | غير موجود — ومبرَّر في أغلبه بحكم النطاق، عدا 4 ثغرات حقيقية (بيئة، توسّع، أخطاء، نشر). | **خارج النطاق جزئيًا** |

**أهم 3 إجراءات من هذا الملحق** (تُضاف لأولوية **P1**):
1. استخراج مكوّن `Card` موحّد يستهلك المواضع الأربعة.
2. تعريف توكنز `fontSize` و`spacing` دلالية في `tailwind.config.ts` وإلغاء تكرار أحجام Tailwind الخام.
3. نقل الدومين لـ `.env` وإضافة `error.tsx` / `not-found.tsx` (متداخلة مع P0).

---

# تدقيق تقني صارم (Strict Audit)

> إعادة تقييم بمعيار صارم: لا يُمنح رصيد على النية ولا على المظهر، والوزن الأكبر لقابلية التوسع
> والتحقق الفعلي. الفارق عن الدرجة السابقة (73) سببه واحد: التقييم الأول احتسب **جودة الكود المكتوب**،
> وهذا التقييم يحتسب **جاهزية النظام للإنتاج والتوسع**.

**Score: 56/100 — Quality Level: Prototype-Grade (غير جاهز للإنتاج)**

| المعيار | الوزن | الدرجة | النسبة | سبب الخصم الأساسي |
|---|:---:|:---:|:---:|---|
| Code Quality & Engineering | 25 | **14** | 56% | صفر اختبارات، ESLint غير مثبّت، البناء الإنتاجي لم يُشغَّل — لا توجد أي بوابة تحقّق |
| SEO & Technical Performance | 15 | **7** | 47% | دومين وهمي يُبطل canonical/OG/JSON-LD؛ لا sitemap/robots/OG image؛ محتوى FAQ خارج الـ DOM |
| Code Clarity & Comments | 10 | **6** | 60% | تعليقان يناقضان الكود، وتعليقات eslint ميتة، ومراجع لمستند غير موجود |
| Documentation | 10 | **6.5** | 65% | README جيد لكنه يحتوي ادعاءً غير صحيح، ولا يشرح كيفية إضافة خدمة جديدة |
| Architecture & Structure | 15 | **8.5** | 57% | لا مسار `[slug]`: النظام مصمّم لخدمة واحدة فقط؛ الأنواع مصدرها ملف بيانات |
| Components & Reusability | 15 | **8** | 53% | عناوين الأقسام hardcoded داخل المكوّنات → المكوّنات غير قابلة لإعادة الاستخدام فعليًا |
| Cleanliness & Maintainability | 10 | **6** | 60% | خرق قاعدة التوكنز 7 مرات، لا Prettier، لا CI، مستودع غير مستقر وقت التسليم |
| **الإجمالي** | **100** | **56** | **56%** | — |

---

**Critical Issues:**

1. **لا توجد أي بوابة جودة على الإطلاق** — صفر اختبارات، و`npm run lint` سكربت وهمي (حزمة `eslint`
   غير موجودة في `package.json` ولا في `node_modules` ولا أي ملف إعداد)، ولا CI، و`.next/` يثبت أن
   `npm run build` **لم يُشغَّل ولو مرة** (لا `BUILD_ID` ولا `.next/server/app`).
   — **الأولوية: حرجة** — **التأثير: لا يوجد دليل واحد على أن الكود المُسلَّم يبني أو يعمل.**

2. **الدومين الوهمي `example-agency.com` مكرر في `app/layout.tsx:29` و`lib/schema.ts:3`** — كل
   `canonical` و Open Graph و`provider.url` في الـ JSON-LD تشير لموقع غير موجود، والقيمة مكتوبة
   داخل الكود بدل متغيّر بيئة فلا يمكن للبناء الواحد أن يخدم staging وproduction.
   — **الأولوية: حرجة** — **التأثير: كامل عمل الـ SEO (Metadata + Structured Data) مُلغى عمليًا في الإنتاج.**

3. **النظام مصمّم لخدمة واحدة فقط** — `page.tsx:2` يستورد `webDevelopmentService` صراحةً، ولا يوجد
   `[slug]` ولا سجل خدمات؛ وبالتوازي عناوين كل الأقسام مكتوبة hardcoded داخل المكوّنات
   (`ServiceCapabilities.tsx:10`، `ServiceProcess.tsx:10`، `ServiceDeliverables.tsx:10`،
   `ServiceBenefits.tsx:10`، `ServiceFaqs.tsx:15`، `ServicePortfolio.tsx:11`)، و`ServicePortfolio`
   لا يستقبل props إطلاقًا.
   — **الأولوية: حرجة** — **التأثير: الخدمة الثانية = نسخ ولصق المجلد بالكامل. ادعاء إعادة الاستخدام
   ينهار عند أول اختبار حقيقي، والمكوّنات "المشتركة" ليست مشتركة.**

4. **مسار التحويل الرئيسي مكسور** — `ServiceCta.tsx:15` يشير إلى `/contact` وهو مسار **غير موجود**
   (404)، و`Button.tsx:20` يرندر `<a>` خام بدل `next/link` فيفقد التنقل الداخلي والـ prefetch.
   — **الأولوية: عالية** — **التأثير: أهم زر في الصفحة — الهدف الوحيد للتحويل — يودّي لصفحة خطأ.**

5. **فجوة منهجية بين ما يوثّقه الكود عن نفسه وما ينفّذه** — README يدّعي *"كل النصوص في ملف واحد"*
   (غير صحيح)؛ `Section.tsx:12` يدّعي أن كل الأقسام تمر عبره بينما `ServiceIntro` و`ServiceCta`
   و`ServiceHero` تتخطاه؛ README يفرض منع الـ hex بينما `ServiceHero.tsx` يكتب `#F2B134` سبع مرات.
   — **الأولوية: عالية** — **التأثير: التوثيق يضلّل المطوّر التالي والعميل — أخطر من غياب التوثيق.**

---

**Priority Actions:**

1. **أولًا (قبل أي شيء):** تثبيت `eslint` + `eslint-config-next`، وتشغيل `npm run build` حتى ينجح
   وتوثيق نتيجته، ونقل الدومين إلى `NEXT_PUBLIC_SITE_URL` كثابت واحد، وإصلاح `/contact`.
   بدون هذه الأربعة لا يُعتبر التسليم قابلًا للمراجعة أصلًا.

2. **ثانيًا:** تحويل المسار إلى `app/services/[slug]/page.tsx` مع سجل خدمات في `data/services/index.ts`،
   ونقل كل `eyebrow`/`title` من المكوّنات إلى ملف البيانات، وإلزام كل الأقسام بالمرور عبر `Section`،
   وتصحيح الادعاءات غير الصحيحة في README وفي التعليقات.

3. **يمكن تأجيله:** استخراج `Card` وتوكنز `fontSize`/`spacing`، وتقليل الخطوط من 3 عائلات إلى 2،
   وإضافة security headers و`error.tsx`/`not-found.tsx`، وProfile الأداء، وStorybook.

---

**Final Verdict:**

الكود **المكتوب** جيد؛ الكود **المُسلَّم** غير جاهز للإنتاج. الصنعة على مستوى السطر ممتازة
(TypeScript صارم، حدود Server/Client مضبوطة، a11y حقيقي، تباين ألوان يجتاز AA)، لكن المشروع
يفشل في كل نقطة تُقاس فيها الهندسة لا الكتابة: لا تحقّق، لا توسّع، لا إعداد بيئة، ورابط تحويل مكسور.

**أكبر نقطة ضعف:** التسليم يعرض *شكل* الممارسات الاحترافية دون جوهرها — سكربت lint بلا linter،
ادعاء مركزية محتوى مع نصوص مبعثرة في المكوّنات، ووعد بإعادة استخدام مع مكوّنات مقفولة على صفحة واحدة.
هذا نمط مميّز للكود المولّد بالذكاء الاصطناعي حين يُقبل دون تدقيق: **يبدو صحيحًا، ويتوقف عند أول
استخدام حقيقي.** المطلوب يوم عمل مركّز على البندين 1 و2 للانتقال من نموذج أولي إلى تسليم إنتاجي.
