import { readWebDevelopmentService, writeWebDevelopmentService } from "@/lib/cms/service-repository";

/** @deprecated Use service-repository directly. Kept for backward compatibility. */
export { readWebDevelopmentService, writeWebDevelopmentService };
